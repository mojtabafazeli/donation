type Province = {
    name: string,
    center: string,
    latitude: string,
    longitude: string,
    id: number;
};

type Option = {
    education: boolean,
    health: boolean,
    misc: boolean
}