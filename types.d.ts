export type Province = {
    id: number,
    label: string,
    name: string,
    active: boolean,
    center?: string;
    latitude?: string;
    longitude?: string;
};

export type Category = {
    id: number,
    label: string,
    name: string,
};