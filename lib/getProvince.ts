export default function getProvince(provName: string): Option {
    // const res = await fetch('./province');
    // return res.json();
    return {
        education: true,
        health: true,
        misc: true,
    }
}
